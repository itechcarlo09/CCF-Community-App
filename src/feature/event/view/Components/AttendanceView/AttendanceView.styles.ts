import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	text: {
		fontSize: 12,
		lineHeight: 16,
	},
	number: {
		fontSize: 18,
		lineHeight: 28,
		fontWeight: "bold",
	},
});
