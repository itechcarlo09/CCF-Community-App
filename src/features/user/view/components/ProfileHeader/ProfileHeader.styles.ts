import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	profileContainer: {
		flexDirection: "row",
		alignItems: "center",
		columnGap: 20,
	},
	nameText: {
		fontSize: 16,
		lineHeight: 24,
		fontWeight: 600,
	},
	ministryText: {
		fontSize: 12,
		lineHeight: 16,
	},
});
